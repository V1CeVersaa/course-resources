## English

Let's get started. So before proceeding with today's lecture, I just wanted to orient since we're a little bit since we're at the end of the quarter now. So this is a final lecture that either I or Tatsu will be giving. So now, let's begin today's lecture. So last time, Tatsu gave an overview of RL from verifiable rewards and he talked about policy gradient algorithms such as PPO and GRPO. This lecture, we're going to deep dive into the mechanics of policy gradient GRPO. So we won't necessarily cover new material,

but just to take the existing material and go a bit deeper and show you code and some math as well. So just to remind us what we're doing. So we're doing reinforcement for language models, and in reinforcement learning you have to define the states and actions and rewards and so on. And just to be very clear, I think this hopefully should be obvious by now. But just to state the obvious. So the state is the prompt plus the response that you've generated so far. And each action is generating a particular token,

the next token. The reward is essentially how good the response is. And for the purpose of this class, we're going to focus on outcome rewards, where the reward is a function of the entire response. We're going to focus on rewards that are verifiable. So the computation is deterministic. Some function just returns a reward as opposed to having to ask a human. And in this setting, typically, in reinforcement learning you see notions of discounting and bootstrapping where it's multiple turns where you're getting.

And that really makes sense when you have rewards in the middle. If this is more of you're just generally a bunch of actions and you see if your answer is correct. So that obviously introduces a bunch of difficulties because it's very sparse and delayed reward. But it also simplifies, I think, some of the conceptual thinking around this. So for example, you might have a question math problem. And then there's a chain of thought you rethink for a while. And then finally, the language model might generate.

Therefore, the answer is three miles. And then the reward would have to extract the three miles and see if that matches some ground truth. So in reinforcement learning, typically people talk about the transition dynamics. It's very important in this case in the language model setting it's very simple. It's just the plus operation. You just append the action into the state. This has a number of important implications because you can-- because you understand the dynamics of the world. You can do planning test time compute.

And this is something that roboticists would only dream of if they had a transition model, dynamics of the world, they could simulate and you could do all these things. In language modeling, we can do that, but not true in general reinforcement learning. And the other thing to realize is that the notion of state is really made up. If you think about reinforcement learning in the context of robotics, the state is the joint angles, the position, maybe an image, or something. It's really much grounded in the real-world.

Whereas language models the state is just tokens that the language model happens to generate. And this introduces a lot of flexibility and introduces a lot of degrees of freedom, where the language model can make up its own scratchpad in order to arrive at the final answer. Whereas often a robot is very much limited by the external world in terms of what states are even achievable. So the issue here isn't about reaching any state, which is often the challenge in robotics and control. You can't even reach the state.

Here you can reach any state you want by just writing down tokens, but the challenge is making sure that these tokens actually lead to some ground truth answer that's correct. So I think it's worth reflecting on. It's all reinforcement learning. But the intuitions and what's hard and what's easy changes in the language modeling setting. So the policy in this case is just a language model action conditioned on the state, which is the prompt and response given so far, this is often fine tuned from an existing pre-trained

language model. And then there's multiple names for this rollout episode trajectory you. In this outcome reward setting, you have to start with a state. You generate a bunch of actions, a.k.a. tokens. You get a single number, which is a reward. And then your objective is to maximize the expected reward where the expectation is taken over prompts which the environment gives you, and also the response tokens which your policy pi that you're optimizing gives you. So hopefully, that makes everything

very grounded and precise. Let me if you have any questions about this setup. So let's now talk about policy gradient, which is a whole class of methods for learning the policy, which essentially start with a policy and try to improve it using gradient methods. So for notational simplicity, we're just going to let a denote the entire response instead of a single action. Because we're in the outcome reward setting, it doesn't really matter. You can just think about the actions as being all generated at once by the language models.

And then you get a single reward at the end. So remember, we want to maximize the expected reward with respect to the policy pi. So just expected reward is integral over the distribution over states times policy of action, sequence given state times the reward. So this is pretty straightforward. And policy gradient says do the obvious thing. You want to optimize something. Let's take the gradient. So you start taking the gradient. So the gradient only touches the policy. And this is key trick. It's like the policy gradient theorem, some people call it.

But it's really just the chain rule for log. So this basically, gets rewritten as a pi and then the gradient of the log. And then I'm going to rewrite it in terms of expectation. So expectation is hiding basically the distribution over SNA here. So this form should be very familiar. So this is the gradient of the expected reward. So naive policy gradient basically says, I'm just going to sample a prompt from P, sample response from A, and then I'm just going to update the parameters based on whatever

inside this expectation. So think about if you're doing SGD, you look at the expected loss in your sample term and you just evaluate and go with that. So interpretation, this is essentially the same as S of t except for everything is weighted by the reward. So an S of t a human being writes down A for you, and you just mimic A and update the parameters to maximize the probability of that action. And now, A is generated by the model. And now-- but you get to hit it with a reward. So just as a intuition building.

So suppose that the reward is either 0 or 1. Is it correct or wrong? So in this case, the naive policy gradient is just going to only update on the correct responses. So R is going to be 0 for the actions that lead to incorrect responses. So those are going to be completely ignored. And then on the correct responses R is 1. So you just essentially sample, you select the responses that are correct, and you just do S of t on this. The only difference is that the data set is changing over time, because after you do an update, now, you have a new policy

and you generate the data set based on that policy and you iterate and so on. But this is very natural. And the only challenge here is-- not the only challenge. The main challenge is that people talk about policy gradient as being high noise or high variance. So in stochastic gradient descent, if you're doing supervised learning, this is also can be the case that your gradient is very noisy. And typically, if you use larger batch sizes and just keep on going, then it empirically seems to work out.

Reinforcement learning is a whole other level in terms of noise and variance that you have to deal with. So in particular, if you look at this setting where you have 0 or 1 as a reward, think about solving a hard math problem. So in this setting is a sparse rewards, the rewards are sparse, which means that very few responses are going to get reward one. Most are going to get 0. So if you have a bad policy and you roll out and you generate your actions, most of your rewards are going to be 0. Which means if you look at the gradient,

you're basically making a 0 gradient update. So if you're in a setting where your policy is so bad that you're not able to get reward at all, then guess what? You're not making any gradient updates and your stuck. So that's a pretty bad setting to be in. At least in supervised learning, you're at least making an update towards something. You're guaranteed to make an update towards something. But in reinforcement learning and sparse rewards, that's not true anymore. Question? Why is the data set changing over time?

So the question is, why is the data set changing over time? So if you think about-- this is your objective function. So you're going to optimize. So you iterate each iteration you're going to compute the gradient and then update the parameters. So the gradient-- computing the gradient involves sampling a response and then updating. So now, your parameters are different. So the next time you sample response it's going to-- might be different now because it's coming from the new model. And if you sample multiple responses those responses that's

going to be a different data set from a different distribution than where you started. So your data set hopefully, is going to get better over time. So intuitively, even if you have this sparse reward, as long as you can get some of the easy questions correct and you can get some reward, you can update your model, and you hope that the model can generalize beyond those examples. So that next iteration, when you're sampling and solving a slightly more difficult questions, you're going to get a bunch of responses that

happen to get the right answer. And now, your data set will have higher and higher reward over time. So this is in contrast to reinforcement learning with human feedback where you use pairwise preferences, you estimate a reward model. And the reward model is generally a bit more continuous. It's going to assign essentially a numerical value to every response how good it is. This is quite different than the verifiable reward setting where it's correct or incorrect. And notice that, I mean, I'm emphasizing this point

that it's all reinforcement learning. It's all even within the scope of language model. Sometimes you even use the same algorithms like PPO, but the intuitions for-- and for maybe how you set the hyperparameters will-- and how you tune the model will be different. So I noticed that the rewards are sparse. So you don't update the parameters if the reward is 0. Is that correct? Then should we just set it to negative 1, so it goes the other direction so we can use those rewards? Yeah. So the question is-- well, in this case if the reward is 0,

then you don't update the parameters. Why not make the reward like minus 1? So at least you're pushing the model away from those incorrect responses. This is something that will happen automatically when we look at baselines. Because there-- this is just a simple naive policy gradient where you're updating against the rewards. We're going to show that there's much better things than just using the reward. Question? I think question-- and then I think there was a question over there. --the reward, only when we got the following response.

The last word of the response. So the reward is a function of the prompt and the response, the whole response. And the response is correct or not only when we generate it completely. So we have gradient only on the last token or? So yeah-- so you're getting at sometimes you have reward before you even finish. And so the other type of reward is called process supervision or process rewards which allow you to basically keep track of how well you're doing. There are things you can do there. And generally, that's better if your process reward is good.

For simplicity, now, I'm just assuming it's an outcome reward. But absolutely, if you have reward earlier, you should always evaluate the reward as early as you can if you trust it. The problem with process reward in language models that you just don't-- it's really hard to get a good process reward because unless you're completely going off the rails, it's hard to tell how well you're doing. Is there another question? Same question. What's the reward model here that we are working with? So how are we getting 0 and 1.

So the question is, what is the reward model? I'll show you some examples of this. But you can think about the reward model which I think for your assignment you're implementing basically the language models generate some stuff and then you parse the answer out and you check the answer against your answer key and you return 1 if it matches and 0 otherwise. So maximize expected reward, policy gradient does the obvious thing. Naive policy gradient-- sorry, does the obvious thing. But you have this high noise and variance problem.

So the-- a key idea in policy gradient methods is using what is called a baseline. And so we recall that this is the expected reward. If you could actually compute this expected reward you'd be pretty good. Because then you could just compute that. But you can't. So you instead use unbiased estimate which usually means ripping off the expectation, sampling of and ripping off the expectation. This is an unbiased estimate, but its variance can be pretty high. So just to give a toy example to appreciate

what I mean by high variance. So suppose you have just two states, so two prompts if you want, S1 and S2. And from S1 you have two possible actions. A1 gives you a reward 11. A2 gives you a reward 9. And from state S2, a1 gives you reward 0 and A2 gives you reward two. So S1 fundamentally, gives you higher reward and S2 just gives you lower reward. And of course, the best policy would be in S1 you want to choose A1 and S2 you want to choose A2. So you don't want S1 to-- and going to A2. But you notice that this 9 is actually larger than this S2.

So you can end up in situations where each state think about this as a really S1 as being a really easy question. And S2 is maybe a harder question. So you just because something gets high reward doesn't mean you absolutely should be updating aggressively against it. So if 9 is greater than 2-- so if you're doing naive gradient updates, you might pick S1 going to A2. And it's like wow this is great. I'm going to get 9 times the gradient. And then I look at S2, A2 and I say well that's only reward two.

So I'm pushing up that less when in fact, if you look at the whole picture it's clear that that's maybe just locally not doesn't really make sense. I mean, I would stress that in expectation. If you look at overall states and actions, it all works out. The math all works out. But you have to look at everything to get that. Does this example make sense? Why would we use A2 in the first case? Because A1 already has 11. So the question is, why do you choose A2? So you're going to choose whatever your policy decides.

So in a policy when you're starting out, you don't which is higher reward. So you're just going to maybe choose something random. So if you happen to choose A2 then you might upweight that and get reward 9. I mean, it's actually worse because you say wow, reward 9. This is great. And you-- the more you choose it, the more you're updating against it. And then you get stuck in this local optimum where you can't even choose. There's no probability of mass left on A1 because you've updated against A2 so much.

So here's the key idea behind baselines is that you're just going to maximize the baseline reward. Instead of expected reward, we're just going to subtract off some B of S called the baseline function. So this is just the expected reward shifted down by a constant expectation of B of S. And the beauty of this is that this quantity doesn't depend on the policy pi. So even if you expand it out, you'll have integral over P of S times pi a given S but integral over a given S is just one. So this is just--

it doesn't depend on the policy at all. And so of course optimizing expectation R is identical to optimizing expectation of R minus this because it's just a constant. So that's the whole idea of baselining. It preserves the optimization problem. And now, if you do naive policy gradient on this new objective then basically you sample a S, you sample A, and then you're just looking at this difference. So notice that this is true, this works for any p of S. As long as P of S doesn't depend on a which

it's not in the argument. So it can't depend A, then this is valid. So now, the question is, well over all possible functions p of S, which one should we use? And let's just revisit this two state example just to provide some intuition. I'm going to make some assumptions. You assume your initial policy is a uniform distribution over all S and A, and your gradient of your policy initially is one just to ignore that for now. So if you look at the variance over your gradients, you'll see that some of the time you get 11, sometimes you get 9,

and sometimes you get 0, and sometimes you get 2, and the variance is 5.3. So if you define a baseline, which is 10 for S1 and 1 for S2, then let's see what happens. So you subtract off the baseline from these reward values. So for S1 you subtract 10. For S2 you subtract 1. So now, the baseline variance is a lot less. It's only 1.1. So we reduce the variance from 5 to 1 which is pretty good by just subtracting a baseline, which, again, doesn't affect your end results in expectation. So I think there is the assumption

that this expectation of P of S doesn't depend on the policy pi. So I was wondering if it is OK still for us to initialize like B as let's say, the initial the very original frozen policy. So a good question. So B of S is not supposed to depend on policy. But your question is, can I just take an initial policy and set B of S to S? So absolutely. So it's one of these things with reinforcement learning you have to be a bit careful with is what's constant and what's not. So you can always take a frozen model and say that's a constant.

And as long as your expression doesn't depend on that, that's actually treated as a constant, you're not differentiating through it, then it's all fine. Which means also that if you are sampling and you're performing the update, that you have to keep an old copy of that policy. Because of hope, if that thing is changing, now, you can get into these weird settings where you're not actually-- it's not actually a constant anymore. And then I will follow up and be like, what if we visualize a pi as the policy from the last step,

the last radius? Instead of the very, very original. So you can do this. So typically, what you do is you essentially have a policy. It's an old policy or anchor for a while. Or if you're doing PPO you have a value function, and then you hold it for a while and then you do some updates and then you update it on. So I mean, in practice is what you do. Part of this, it can't depend on, is really like for the theory to work out. But in practice there's all things that people do. So what we showed is that at least for this example,

anecdotally you can define some baseline that reduces the variance. So this is giving you hopefully, a concrete sense of the power of a baseline. In the lower variance you have the faster the convergence. So you might wonder what is the optimal baseline to use. And if you do some math, you realize there's actually a very nice closed form solution for this. This is written for one parameter models. If it's multi-dimensional I think you have some covariances. And this is just expectation of this derivative squared times

R divided normalized by the same expression without the R. So this is in general annoying to compute especially if you have high dimensions. And because this thing is going to be some of you covariance. So typically, what people do is you just ignore this stuff and you say, well, I'm just going to try-- I'm going to set baseline equals expected reward divided given S. So this is not optimal. But it's a good heuristic. And even this is-- you can't compute this. Because anything with expectation you can't compute.

You can only estimate. But this is now I think a good guiding principle for what you want your baseline to be. It's the expected reward given your state. So just to recap, we said we have the naive policy gradient. We can introduce this baseline as valid for any B of S. And now, we've come to a conclusion that maybe this is a good choice if we can try to get a handle on expected reward given S. So this particular choice has connections to advantage functions. So in reinforcement learning, you typically

have a value function, which is the expected reward given a state. And then you have a Q function, which is expected reward given a state. And I've taken a particular action. And note here that Q and R are the same because we assume outcome rewards and a has all the actions. In general, R is going to be like the returns from that state rather than just a single reward. Those two are same for now. And the advantage function is just defined as the difference between the reward I would get if I took action and--

sorry, if I average over possible actions in. In particular, it's asking-- it's measuring how much better is action A than the expected value that I get from S. What is the advantage of taking A as opposed to just going along with the baseline-- sorry, baseline is ambiguous. But we're going with the policy. So then if you look at B equals this heuristic form that we talked about earlier, then the baseline reward where you just subtract off mu of S is identical to the advantage. So you can interpret the baseline

if you have the advantage-- sorry, if you have this choice of baseline is identical to optimizing the vantage. So that's just to give you some intuition behind what this baseline is doing. In general, we are probably going to deviate from that because we can't estimate any of-- we can't compute these expectations exactly, so we're going to do something else. So in particular, I'm just going to let delta denote whatever thing we put in to the estimate. So all of the methods are going to basically take

the gradient of the log policy and times something that's going to be based on the reward. So all the algorithms see, your PPO, GRPO, have this form where you're basically you rolled out and got an action. And now, the question is, how much do you want to move towards that action? And it's going to depend on the reward. So you can put the reward here. Then that's naive policy gradient. You can do reward minus some estimate of the expectation. And you can also do divide by the standard deviation,

which is what GRPO does which we'll see later. But this is the general form. And so the exact form of prior isn't that important because next year there's going to be GRPO 2 or whatever. But all of this policy gradient and stuff is I'll be giving the same lecture next year. Speaking of lecture, you can check out Chelsea's deep RL class for a few more derivations and intuitions about RL in general. So any questions about policy gradient and baselines and advantages? Just for the baseline like a baseline reward.

Is that what baseline? So what's the definition of a base-- a baseline is any function that B of S which only depends on S and A. That you're essentially sticking into a policy gradient algorithm. You can think about it that way. So basically, you update policy gradient will update based on this. And baseline is whatever function you choose there. So those of you who have seen control variants, this is basically the same idea from statistics. So now, let's try to dig a little bit deeper. We're going to actually define a simple model, a simple task,

and actually walk through some code that may or may not look like assignment five. And we'll try to hopefully do some runs. So a lot of this exposition is based on the GRPO algorithm. So this slide I might have to update next year. But it's trying to generalize it beyond the particular choices here. So GRPO remember what Tatsu said last time, it interesting lineage where you had PPO and then PPO and GRPO is simplification of PPO as opposed to algorithms. You start simple and then grow in complexity.

But really, I think GRPO exists in 2024 as opposed to in 2000-- I think it was '17 or something when PPO was introduced is, because the language model setting provides some natural structure that motivates GRPO. You can't really do GRPO naturally, I think in classical RL settings with the robot trying to walk. And the particular thing is that in the language modeling setting, you have a prompt, you can generate a bunch of responses from that prompt. And this gives you a natural group structure to compute essentially the baseline, which

turns out to be the average over the rewards. The whole point is that you're trying to minimize the variance. And the group is a natural comparison set. That's why this is relative. It's relative to the group. Whereas if every rollout looks different there's no natural group structure, then GRPO doesn't really make sense. And you have to find something else and that's something else, generally, tends to be a value function, which aggregates over all the possible states and actions. And here in this case, you have an empirical estimate

over all the possible responses without considering everything because of the LM setting. So here's the pseudocode. We're going to look over it in actual Python in just a second. But I'll just flash it up here. So let's define a simple task. So I wanted something simple enough where I can actually train a very simple model on my laptop without-- and running a language model transformer wasn't going to. I mean, I don't have a fancy laptop, so that wasn't going to happen. So here's a simple task, sorting n numbers.

So the prompt is n numbers and the response is n numbers that are hopefully sorted. So now, you have to define a reward to complete the definition of the environment. And the reward should capture how close to the sorted ground truth the response is. So immediately, you actually see one of the interesting things in reinforcement learning is that you can define a reward in different ways. So for example, you can define a reward which is one, if it's sorted and 0 otherwise, which is presumably what you care about.

But you're going to remember the sparse reward problem. If you start out with a policy that doesn't do anything, you're mostly going to get 0 reward, and you're not going to really make much progress. So let's define a reward that gives partial credit. So we're going to define a reward that returns the number of positions where the response matches the ground truth. So you-- basically, the code looks like this. You look at the ground truth, which is the prompt. And you just count the number of positions where the response

matches the ground truth. So the reward for the correct answer is 4 because 4 positions match. If you have this, thing that doesn't really solve the task, then reward is one. Because I think you got lucky and you put a 2 in the second position. And you have this thing which also is not solving the problem. But you look at this and say, well, it's really trying. And again, you get a 1 because the 0 is in the right position. So that's one reward you could use. Maybe you're unhappy with this because this is pretty far off.

And this is closer. But yet they're all getting reward one. So let's define an alternative reward that gives more partial credit. So what I'm going to do here is I'm going to give one point for each token in the prompt that shows up in the response. So at least you output the tokens that are in the input. Not too much to ask. So for this particular example, I get an inclusion reward of four. And then I'm going to get one point for each adjacent pair in the response that's sorted. So that's a 3, because there's 3 pairs of adjacent pairs.

And the reward is the sum. So that's 7. So if you're paying attention you realize that this reward is actually has a loophole in it. But I'll let you figure that one out. So now, this particular very wrong answer, it gets a reward of 3 and this one gets a reward of six because it's really trying its best. And we give a lot of partial credit for RL algorithms. So that's the task. And in general, we're going to go with the second reward function. So the task is clear. That's an environment. So now, let's define a model to solve this task.

And so we're going to define a really simple model. We're going to assume the prompt and the response length are fixed and of the same length. When I was trying to iterate on examples, there was one-- I was doing some of dynamic thing and it was just like annoying to do. So I simplified it. I'm going to try to capture positional information with separate position parameters, because obviously, position is the name of the game here. Or position matters for sorting obviously. And one thing I'm going to do is decode each position

in the response independently. So this is not a regressive model. I'm just going to encode the sequence and I'm just going to output all the tokens separately. Not what you want to do generally in real life, except for I guess in some speculative decoding it can be helpful to do this in limited doses. But it makes the code a lot cleaner because this autoregressive generation. Now, that's the part which was getting really hairy. So despite this simplify it. So here's the model. So I have the embedding matrix, I have--

for each position, I have a matrix for basically transforming for decoding and a matrix for-- sorry encoding and then decoding. So that'll be easier to explain as I look at the forward pass. So I start with a prompt. I'm going to use this notation in general to denote the dimensions of the tensor here. So there's a batch dimension, which is the number of examples and the number of prompts. And then a position dimension, which is obviously the position I'm in. So I'm going to generate responses.

So this is inference. So let's walk through this. So just some terminology. So I have a batch size which is the number of prompts here. Each prompt has a particular length 3. Number of responses per prompt I'm going to generate in this case 2. And the response length is 4. In the sorting task this response length is going to be identical to the prompt length. So first I'm going to pass the prompts through the model to get logits, which is going to give me for each prompt and each position a distribution over tokens.

So what this looks like is I'm going to embed the prompts into this dimensionality. And now, I'm going to transform using the per prompt position matrix. And I'm going to collapse it. So using an enzyme I have batch position dimension 1. And then this weight is per every position I have this dim 1 to dim 2 transformation. And I'm going to end up with batch dim 2. So I've summed out the position and dim 1. And then given that vector for each prompt, I'm going to for each position apply this matrix

and get back a vector. And then finally, given a vector for each position in prompt, I'm going to convert that into a logits over my vocabulary. So pretty-- I mean it's a simple model I made up. I don't know if it is actually any useful-- anything useful. But it fit this did this toy example. So now I have logits. And this is just shenanigans to deal with the fact that torch multinomial needs something flattened. So I'm going to essentially basically flatten this into a batch times position by vocab.

I'm going to sample responses. And that's going to give me batch position number of responses which I'm going to call trial is that dimension. And I'm going to basically rearrange things to make it so that it's for every prompt, for every trial, I'm going to get a sequence of examples. So maybe I'll pause just to check that everyone's with me. So this is nice. This is usually where you would call out to VLM and do the inference. I'm just doing this here because it's a very simple model. And I can just everything is just like matrix operations.

The responses are going to be very close by. We are sampling from the same logits. And responses are going to be close by. The responses are going to be close by. So we're sampling responses independently from that distribution. So they're definitely going to be similar. But that would be the same as if you sample from a language model as well. The only difference between a language model is autoregressive and this is independent. Trust me. The point was, shall we change the temperature, basically?

Yeah. So you could change the temperature. You can do various things. I'm trying to be faithful to the algorithm. So I get my responses. I can compute a reward over all these responses. And this basically, goes through. And for every prompt and every response I just call this reward function, which we looked at earlier. Nothing too interesting there. And-- sorry, this is a little bit long. Oh, no. Well this part is going to be far less interesting because I thought I fixed the random seed. Generally, you wanted a little bit more variance

in the rewards. Anyway, so the rewards all happen to be 3. So this is going to make some of this other stuff less interesting, but nonetheless, I'll go through the computation here. So there's this function called compute deltas, which takes the rewards and converts them into the delta that I'm going to use for my updates. And the framework is there's many ways-- many choices here, some of which you will explore in your assignment. So for example, if you can just return delta equals the rewards,

you can center the rewards. So you can compute the mean over all of the responses for each prompt. So this is what this is doing. And then you can just subtract off the mean that gives you center rewards. And to answer a question from earlier, this is where if your rewards look 1 and a bunch of zeros, your average is going to be like point, let's say 0.1, which means that if you subtract that, now, you're going to end up with a bunch of minus 1's and something that's slightly below 1. And therefore, you're going to perform

updates on all those negative examples as well. So that's central rewards. And then you can do this thing in GRPO where you normalize. So you center the rewards, and then you divide by the standard deviation and add something small. So you don't divide by 0 just in case. And you return that. So one thing to notice is that what happens if all your rewards are 5? For example. So rewards is just going to be 5. So you're going to make a bunch of updates, centered rewards is going to be all 0. So that means you're not going to make any updates.

So this is interesting. Think about if you have an example where your policy is going generating everything is the same reward. Then you're actually not going to make any updates on that example. And the intuition, it might or may not make sense depending on what you're trying to do. But the intuition is that there's no gradient. You're not-- there's-- in terms of relative comparison between that group, there's no reason to favor any particular example more than another one. So you're just going to abstain and hope

that some other examples give you some signal. And the normalization means that if all your rewards are, let's say, 10 versus all the rewards are-- if you multiply all rewards by 100, then the normalized rewards is invariant to the scale of the reward. So I think that's the good thing about that. Just for fun, there's another thing which the paper didn't do, but you might think is a cool thing to do. So you can 0 out any reward that isn't the maximum for each batch as well. So the idea here is that often in reinforcement learning,

sometimes you really get into these local optimum where if you assign too much partial credit, then the policy is going to latch on to that and go after the low hanging fruit instead of the thing that would give you more reward. So you could try to just say, well, if it's not getting the maximum reward, then I just don't want that. It's an all or nothing thing. So far what we've done, we generate a bunch of responses, we compute rewards. And these deltas which allow me to provide the update, now,

I need to look at the other piece, which is the log probabilities of the responses. Because remember in the gradient-- policy gradient I have delta times the gradient of the log policy. So this part is also fairly straightforward. I pass this through the model, convert the logits into log probs. And then there's some shenanigans to because the prompt is doesn't-- is only batch pause and responses has an extra dimension in there. So I'm just going to upcast these log probs to that higher tensor, dimensional tensor.

And I'm going to index log probs using the responses. So the responses basically for every batch trial position specifies the index into log probs that I need to get. So this is batch trial pause vocab. And this is batch trial pause with the index. And that will give me batch trial pause. So this gives me-- so logits that are returned by the model give me a distribution over all the vocabs for every position and prompt. And now, I have log probs for the particular responses. So these are log probs for these particular responses I got.

So now, we want to finally compute the loss. And the loss is just going to be-- and here there's another set of choices here. So I'm trying to present this as a menu of sorts. So the most naive thing is-- this is naive policy gradient. You just take the deltas and the log probs and you just multiply them together in average. So this is log probs. Remember batch trial pause and deltas is batch trial. And this is because it's outcome reward. You only get-- there's no position dependence on the deltas.

So basically, every position gets-- the deltas get broadcasted to every position. If you had process reward then this deltas would be per position as well. So that's the naive thing. Just to interlude to make sure that we into this idea of freezing parameters. So later we'll see in GRPO, you see these ratios. This relates to this idea that you have a probability of action given S and you're dividing by some other model. And it's really important that you can remember to freeze and not differentiate through p_old.

So just to give you a toy example. Suppose I have a single parameter, w, and that drives both the probability. Think about this as a policy. And also there's an old value, which in this particular iteration is just the same thing. And now, what happens if I compute the ratio and do a backward pass. What would the gradient be? So I'm dividing p by p old. So the ratio is going to be one, and the gradient of one is zero. So what are the-- so it's zero which is obviously not very helpful. So the way you do this properly, is

that you have your p as usual, and then anything you want to treat as a constant, you put in wrap in this no grad. And then this allows you to get the value of-- but not the whole computation graph. And you just divide by that. And now, the gradient is not zero. So just like a be careful when you're-- because in RL as opposed to when you're doing pre-training, there's many different values that all depend on different either old or not old parameters. Why don't we discount an outcome reward when we go back to the sequence?

I guess what do you probably have a hypothesis that the last 100 tokens matter more than the first 100? So the question is, why don't you discount-- do some of discounting when you have outcome rewards? And maybe the last 100 tokens matter more than the first 100 tokens. I think that is less not so clear. I mean, certainly, like the last tokens where you're generating, literally generating the answer is important. But I think the problem is that in reinforcement in general, you have to do credit assignment.

There's certainly decisions early on that matter. In fact, they might even matter more than what you get. Because if you basically have the right strategy, then the stuff that happens at the end, maybe just follows out. And the hard part in reinforcement learning is that you don't where to do the credit assignment, especially when you have a sparse reward. So I think in this setting, we're just going to smear the credit all over or the blame all over the place. So with that interlude, so this is

important for computing the GRPO loss. So imagine we have an old model with an old-- so this is an old checkpoint. And using this old model, we're going to compute the log probabilities of the responses. So that gives us the old log probabilities. And now, let me just skip the unclipped version. So this is the GRPO loss. So let's step through it. So it's the clipped version. So you look at the log probs and you divide by the old log probs which remember should be a constant. Actually, I didn't follow my own advice.

I don't think I wrapped the old log probs in with no grad. But anyway, you know how to do that. So you look at this ratio and then that ratio gets multiplied by delta. And so if you can trust with the naive thing, you're just multiplying the log probs times delta. So this is basically the same thing, except for I've divided by all log products, which is a constant. So I'm just scaling the gradient. But now, there's another part which is I'm clipping. And the reason I compute this ratio is in the first place

is that I'm going to clip the gradients between 1 minus epsilon and 1 plus epsilon. So this is to make sure that the ratios don't go too out of whack. And I'm going to also multiply these clip ratios by delta. And I'm going to take the minimum and return it. And there's a negative sign because I'm talking about rewards I convert that into a loss. So this is just following the math in your GRPO paper. And in the assignment, you'll see this explanation of why this makes sense. So if your updates are within the clipping range,

then you just get the standard update. But if your ratio is outside, then the magnitude of your updates are going to be bounded by 1 minus epsilon and 1 plus epsilon. So there's also this KL penalty, which provides additional regularization. And so just to a quick little math exercise here. So remember the definition of KL between two distributions. It's your sample from the first left distribution and its log over the left or the right. Rewrite that as you Q over P with a negative sign. And then-- so if you're doing KL penalty,

you could just use this directly. But there's this-- and if you want to estimate the KL, you can just take strip out the expectation and you get an unbiased estimate. It turns out that this particular estimate can have lower variance where you just do Q over P minus the log minus 1. So it looks a little bit weird, but you can check that this is identical. Everything's equal because expectation Q over P is just 1. So this part is just 1. And you subtract 1, which is 0. So you're just left with this original term.

So the math works out and the inside just gives you a better estimate of the KL than doing this naive thing. So a lot of cases, when you're trying to find these estimates of the expectation, you're just trying to find an unbiased or sometimes even biased estimate that has lower variance than just doing the naive thing. So you try to rewrite the inside to-- so that it reduces the variance. So just a quick implementation of the KL penalty. This is pretty straightforward. The only thing you have to pay attention

to as you're summing over the last dimension but taking a mean. So you're summing over the vocab but you're taking a mean over batch trial in position. So just to summarize so far the components that are needed to introduce the actual algorithm, you generate the responses given a particular fixed model. From the responses, you compute the rewards and the deltas. And in the case of GRPO and friends, these don't depend on the model. It's just a function of the responses. And if you think about architecting this system,

sometimes the reward is very simple. It's just like a one liner exact match. Sometimes the reward will involve an agent, executing an environment and doing all crazy things. So this could be expensive or cheap operation, but it's independent of the actual model, unless you have LM as a judge, which case, that's another model. Simultaneously, you can compute the log probs of the responses, and then you compute the loss from the log probs and the deltas that you got here and you combine them either clip or not clip or you normalize or you--

sorry, that's up here. So you either clip or don't clip and then you take a gradient step. So let's try to put this all together. So this is the full algorithm, which is essentially a implementation of what's in here in code. So there's some boilerplate, I'll just skip over. So I'm going to define some trivial data set here, 3 prompts of length 3 each. And then define the model. Optimizer, and then I have this-- there's multiple loops here and GRPO. So there's an outer epoch loop. And here, if I'm doing the--

sometimes if I'm using the doing a KL penalty, then I'm going to freeze the model and have that as a reference so I can regularize towards that. And then otherwise I sample the responses. So we talked about this. We compute the rewards. We compute the deltas depending on what variant flavor you like. And if you're doing the KL penalty then you also need to compute the log probs under the reference model. And if you're doing not-- if you're doing some of clipping, then you need to compute also the log

probs under the current model. And then you take a number of steps given the responses. So the idea here is that often response. Remember inference is expensive. You generate and you want to do a bunch of gradient steps with respect to the responses. So this inner loop is basically taking a bunch of steps with respect to the same set of responses. And to do that, you compute the log probs. You compute the loss. If you want, you can add a kl penalty, which sometimes helps, sometimes doesn't really make a difference.

And then you just optimize. So that is-- maybe I'll stop and answer any questions people have about the setup here. So the big picture is that you have the outer iteration, which you ignoring the kl penalty because there's another wrinkle I'll mention later. So you generate a bunch of responses. And then you quickly do a bunch of iterations on them. And you generate a bunch of responses and do some iterations. That's the basic idea. Now, there's an additional thing which if you have the kl penalty, then you actually regularize towards a reference

model, which is changing even slower than this outside model. So the way this is written is actually slightly different than the GRPO code. In the code, you actually have three loops. I only have two because I'm just updating the reference model a little bit infrequently. And I'm also not doing this part. So there's a few implementation details, but hopefully, you could get the rough shape of what this looks like. So you actually have of three models. You have the reference model that you're doing

cl regularization against. And then you have the old model which you're using to compute these importance ratios between the current model and the old model which you're also freezing. And then updating the current model online. So one annoying thing about the reference model is that you actually have to store a whole copy of the model, which can be just doubles your memory. The old model is actually a little bit better because you just remember if in the code, you actually don't have to copy the old model,

you can just generate a bunch of responses and compute the log probs on those responses and just store the log probabilities, because those are going to be-- you're dealing with the same responses in an inner loop. So you don't have to store a whole copy of the model, you just have to store the log probs. Question? Why don't we do kl divergence between old policy and the latest policy instead of doing it against your reference? So the question is, why don't you do the kl penalty against the old instead of the reference?

The way to think about this is that-- let's see. So remember how we said we define objective function in reinforcement learning. And we optimize that. It's a little bit of a lie. Because really you just define this objective function, so you can compute a gradient. And the gradient you compute and you update for a while, but then your objective function changes. So really you can think about let's say-- so this point you fix a reference model and then you write down this nice objective function, which is--

let's see if I don't have the full expression here, but the expected reward with all the expected reward and then the kl penalty. So if you change that to quickly, then-- I guess the more you change that, then the less you're actually like optimizing that objective function. So that's I guess maybe one reason. I mean, honestly, I think empirically you-- it's a gradient because depending on how many steps you take here and how often, you can make it either more like pi old or less like pi old. I mean, you have two mechanisms for regularization.

One is the clipping thing, which is applied to pi old, and then the KL which is applied to pi ref. Question. For the first iteration GRPO for the most inner loop isn't pi theta old equal to pi theta, is it wasteful to do that? So the first iteration pi is equal to pi theta. So you're essentially going to-- well you're still performing an update. You're just scaling by some. Remember, pi all of this from the point of view of the update is just a constant. And I guess the only thing that matters is that you're not going to get clipped because the ratio is 1.

So you're just going to guarantee to make an update. So yeah, it's fine. So maybe this is just round this out a bit. So actually-- so I ran some experiments. So you run the same piece of code that I presented. I'm going to do 100 epochs number of steps for epochs 10. And we see this learning curve. So just to maybe walk through what this is doing. So on epoch 0, step 0, we have this prompt. The responses it generates a bunch of stuff. You score them, you compute the reward, and then you compute the delta.

In this case, it's identical to the reward. So I'm going to update towards the 3's a little bit more than the 2's. And then you see this prompt the same thing. This one-- I'm still going to update against all these-- towards these responses even though they all have reward 3. And then let me scroll down and see what happens. Eventually, you see that I'm getting reward 3. But this is not looking-- I mean, it's not really sorting is as short answer. It's producing numbers which are more or less in the prompt,

but not really sorting. Let's see how it does at the end. So it's not terribly great. I actually haven't looked at the other examples. So it'll be exciting to see what happens here. So here, we're using the centered rewards and see the mean reward is getting higher. So it's like past 3 instead of 2, which is promising. So let's look at this run. So notice here that I generate some examples. Reward is 2 and 3. But because I'm centering, I'm giving-- pushing them all towards the ones with reward 3

and away from the ones that have reward 2. So this is what centering is doing. And if they're all the same, then centering doesn't really-- is actually going to be a no op. Whereas before I was performing updates which was silly I think. Because if you think about it, I'm sorting 1, 2, 3, and here are all the responses are equally bad. So updating towards these don't really maybe make as much sense. Let's see towards the end how this does. So still not, I guess it's not great but it's got a little bit higher reward.

I probably need to tune this a little bit more. But at least it's getting reward for here for getting the right numbers. And 0, 1 it's not the most unsorted. Here-- I think it's getting the permutation. And here, I think it just gets stuck. And at this point, you see all the deltas are 0. So more training isn't really going to help on this batch. But if you regenerate, maybe you'll get some fresh samples and be able to update. So you can see that it's the partial reward is a double edged sword.

Because I'm giving you four points. So it's like, it's a good reward. But I still didn't solve the problem completely. So I think you have to be careful with reward, additional reward. And then finally, the standardized-- I think the normalization here didn't really do all that much. So I'm going to skip it. One thing to I think note is that if you look at these loss curves. You think, well, this looks good, but why is this looks pretty bad even though the reward is going up. And this goes back to my statement

that minimizing the loss is a little bit of a lie here. It's not like we were minimizing one loss function that you can measure over time, because the set of responses is changing over time. You don't have really a reference point for the same loss. I mean, you can measure loss on maybe some validation set. But if you only have rewards, then you're back to only being able to rely on reward. Because even-- think about it, even if you have a bunch of examples where you're the model is just happy generating them.

It's like the loss is only respect to what the model is generating. So it's a self circular thing. So monitoring the losses not really such a meaningful thing to do. So maybe just to wrap up here. So reinforcement learning, just popping back up, I think is really exciting because it's really I think the key to making models that actually surpass human abilities in some sense, because labeled data is only going to get you as far as mimicking the behavior that's presented in the labeled data. I think the model is if you can measure it, you can optimize it.

If you have a reward. I think there is optimization as we've seen even with this sorting examples, I mean can be challenging in itself. But there's also perhaps a bigger problem is, how do you design rewards that are not hackable and environments that are generalizable. I think that's a pretty active area of Investigation. For the optimization part, I mean, the sorting isn't that hard. I think I just ran it for seconds on my laptop. So you shouldn't expect greatness. I think just to review the policy gradient framework,

I think is conceptually clear. You write down the expected reward, you subtract a baseline, it's the same thing. And then you just take you try to find estimate of that reward or advantage. And that's one thing that we didn't talk about, which I think is actually important. And maybe next year I should try to do a bit more of this, is that building RL systems and scaling is actually much, much more complicated than pre-training. And the reason is that you have to do inference and inference is its own can of worms.

You also have to manage multiple models. So you have the policy you're training, you have-- if you have PPO, then you have the critic. If you have the inference and you have maybe an inference worker, then you need to ship the model over there to run inference. If you have an agent environment, you have the environment which requires spinning up. And then on top of that, you also have the pi old and pi ref. So you have multiple models that you need to keep around. And on top of that have to do everything

in parallel and distributed and make it all sing and dance. So there's a lot to be done there, which we're not really going to have time to talk about in this class. But if you're interested, there's definitely pointers I can send you to. So that's it and see you next time.

## 中文

那我们开始吧。 在继续今天的讲座之前， 我只是想说明一下情况，因为我们现在有点——因为我们 现在学期快要结束了。 所以这是我或 Tatsu 的最后一堂课之一。 将要讲的内容。 那么现在，我们开始今天的讲座。 上一次，Tatsu 对强化学习做了概述， 从可验证的奖励出发，他谈到了 诸如 PPO 和 GRPO 之类的策略梯度算法。 在本次讲座中，我们将进行深入探讨 政策梯度 GRPO 的机制。 因此我们不一定会涵盖新的内容， 而是对现有内容做更深入的剖析 并向你展示代码以及一些数学推导。 所以先提醒一下我们在做什么。 我们正在对语言模型进行强化学习， 在强化学习中，你 必须定义状态、动作、奖励等等。 并且为了非常清楚起见，我想这希望是... 现在应该很明显了。 但还是把明显的说清楚。 因此，状态就是提示词加上你到目前为止生成的回复 每一个动作就是生成一个特定的标记（token）， And each action is generating a particular token, 下一个标记。 奖励本质上就是回复的好坏。 在本课的目的下， 我们将专注于结果型奖励，其中 奖励是整个回复的函数。

我们将专注于可验证的奖励。 因此计算是确定性的。 某个函数只需返回一个奖励 而不必去询问人类。 在这种情形下，通常在强化学习中 你会看到折扣和自举（bootstrapping）的概念 在多回合互动中你会不断获得奖励。 当你在中间收到奖励时，这种做法确实很有意义。 如果更多是你只是进行一系列动作，然后查看你的答案是否正确。 and you see if your answer is correct. 这显然带来了许多困难，因为这是非常稀疏且延迟的奖励。 但我认为这也简化了在这方面的一些概念性思考。 例如，你可能会有一个数学题问题。 around this. So for example, you might have a question math problem. 然后会有一条链式思路，你会思考一会儿。 然后最终，语言模型可能会生成。 因此，答案是三英里。 然后奖励函数必须提取出“三英里” 并查看那是否与某个真实值相匹配。 所以在强化学习中，通常人们 会讨论转移动力学。 在语言模型这个情形下这点非常重要 而且非常简单。 它仅仅是加法操作。 你只需将动作附加到状态中。 这有许多重要的含义

因为你可以——因为你理解 世界的动力学。 你可以在测试时进行规划计算。 而这正是机器人学家们如果拥有转移模型、世界的动力学，他们只能梦寐以求的事情， 他们可以进行模拟， 你就可以做所有这些事情。 在语言建模中，我们可以做到这一点， In language modeling, we can do that, 但在一般的强化学习中并不成立。 另一个需要意识到的是，“状态”这一概念 其实是人为设定的。 如果你从机器人学的角度来思考强化学习， 状态是... 是关节角度、位置，也许还有一张图像， 或者别的什么东西。 它确实更贴近真实世界。 而对于语言模型，状态 只是语言模型恰好生成的标记（tokens）。 这带来了很大的灵活性 并引入了许多自由度，使语言模型可以自行构建临时笔记 以便得出最终答案。 而通常机器人则非常受限 Whereas often a robot is very much limited 由外部世界决定哪些事物是可实现的。 哪些状态是可达成的。 所以这里的问题并不是关于到达 任何状态，这通常是机器人技术 和控制领域中的挑战。 你甚至无法到达该状态。 在这里你可以通过仅仅写下标记来到达任何你想要的状态

但挑战在于确保 这些标记实际上会导致某个真实的、正确的答案。 answer that's correct. 所以我认为值得反思一下。 这归根结底都是强化学习。 但直觉以及什么是难的 和什么是容易的，在语言建模的情境中会有所不同。 因此在这种情况下，策略只是一个语言模型的动作 在给定状态（即提示和生成的回复）的条件下 到目前为止，这通常是从已有的预训练语言模型进行微调得到的 而且 这个 rollout 片段/回合有多种命名方式 轨迹（trajectory）也可以这样称呼。 在这种以结果为奖励的设置中，你必须以一个状态开始。 你生成一堆动作，也就是标记（tokens）。 你得到一个单一的数值作为奖励。 然后你的目标是最大化期望奖励 其中期望是针对提示（prompts）所计算的， 环境给你的，以及你的策略 π（你正在优化的）给你的响应 以及代币（tokens）。 所以希望这能使一切 非常有依据且精确。 如果你对这个设置有任何问题，请告诉我。 那么现在我们来谈谈策略梯度，这 是一整类用于学习策略的方法，它们 本质上从一个策略开始并尝试 使用梯度方法来改进它。 为了符号上的简化，我们正在 我们就让 a 表示整个回复吧

而不是单个动作 因为我们处在结果奖励的设定中 这实际上并不重要 你可以只考虑这些动作 被认为是由语言模型一次性全部生成的。 然后你会在最后得到一个单一的奖励。 所以记住，我们要最大化关于策略 pi 的期望奖励。 因此期望奖励就是对该分布的积分。 So just expected reward is integral over the distribution 关于状态乘以策略的动作、序列 给定状态乘以奖励。 所以这相当直接。 而策略梯度说要做显而易见的事情。 你想要优化某些东西。 我们来对梯度取导。 于是你开始取梯度。 因此梯度只作用于策略。 这是关键技巧。 有点像策略梯度定理，有些人是这么称呼的。 但它实际上只是对数的链式法则。 所以这基本上被重写为 作为一个 pi 然后是对数的梯度。 然后我将把它重写成期望的形式。 因此期望基本上隐含着分布。 在这里取自 SNA。 所以这个形式应该非常熟悉。 所以这是期望回报的梯度。 因此天真的策略梯度基本上就是说， 我只会从 P 中采样一个提示，然后采样 来自 A 的响应，然后我只是 将基于该期望内的任何内容更新参数。 所以想想如果你在做随机梯度下降（SGD），

你会查看样本项中的期望损失。 you look at the expected loss in your sample term 然后你只需评估并照此进行。 因此解释起来，这本质上与 S(t) 相同 只是所有内容都按奖励加权。 所以 S(t) 人类为你写下 A， 你只需模仿 A 并更新参数以最大化 该动作的概率。 现在，A 是由模型生成的。 现在——但你可以用一个奖励来打击它。 所以仅作为直觉上的构建。 所以假设奖励要么是 0 要么是 1。 这是正确还是错误？ 所以在这种情况下，朴素的策略梯度 只会仅对正确的响应进行更新。 因此对于那些导致不正确响应的动作，R 将为 0。 lead to incorrect responses. 所以那些将被完全忽略。 然后对于正确的回复，R 为 1。 所以你基本上只是抽样，你 选择那些正确的回复， 然后你只对其做 S(t) 。 唯一的不同是数据集会随着时间而变化， 因为在你进行一次更新之后，你现在有了一个新策略 于是你基于该策略生成数据集 然后你不断迭代，诸如此类。 但这是非常自然的。 而这里唯一的挑战是—— 并不是唯一的挑战。 主要的挑战是人们 谈论策略梯度时常说它

噪声大或方差高。 那么在随机梯度下降中， 如果你在做监督学习， 这种情况也可能出现，即你的梯度 非常噪声大。 通常，如果你使用更大的批量大小 然后就一直继续下去，经验上似乎能奏效。 强化学习是另一个层面的问题 在噪声和方差方面需要处理的东西更多。 所以特别是，如果你看这个情形 当奖励是 0 或 1 时， 想一想去解决一个困难的数学问题。 所以在这种情形下奖励是稀疏的，奖励 是稀疏的，这意味着很少有回答会 得到奖励一分。 大多数会得到 0 分。 所以如果你的策略很糟糕，而你进行采样 并且生成你的动作，大多数奖励 将会是 0。 这意味着如果你看梯度， 你基本上在做一个零梯度更新。 所以如果你的策略处于 非常糟糕以至于你根本得不到任何奖励的情况下， 那你猜怎么着？ 你就无法进行任何梯度更新，而且被卡住了。 所以那是一个相当糟糕的情况。 至少在监督学习中，你是 至少会朝着某个方向进行更新。 你被保证会朝着某个方向进行更新。 但在强化学习和稀疏奖励的情况下， 情况就不再如此。 问题？ 为什么数据集会随时间变化？ 那么问题是，为什么数据集会随时间变化？ 所以如果你考虑一下—— 这是你的目标函数。

所以你要进行优化。 所以你在每次迭代中都会 计算梯度然后更新参数。 因此，梯度——计算梯度 涉及对一个响应进行采样然后进行更新。 所以现在，你的参数变了。 因此下一次你采样回应时它会—— 可能会不同，因为它来自新模型。 如果你采样多个回应，那些回应将会是 来自不同分布的不同数据集 比你起初的位置更好。 所以你的数据集希望会随着时间变得更好。 因此直观上，即便你只有这种稀疏的奖励， 只要你能把一些简单的问题答对 并获得一些奖励，你就可以更新你的模型， 并且你希望模型能够泛化 超越那些示例。 这样在下一次迭代，当你 抽样并解决稍微更困难的问题时， 你会得到一堆响应，这些响应 碰巧得到正确答案。 现在，你的数据集随着时间推移会有越来越高的奖励。 因此，这与使用成对偏好的有人类反馈的强化学习形成对比， So this is in contrast to reinforcement learning with human feedback where you use pairwise preferences, 你会估计一个奖励模型。 而奖励模型通常更倾向于连续值。 它基本上会为每个回复分配一个数值

来表示它的好坏程度。 这与可验证奖励有很大不同 在某些情况下它是正确的，或是不正确的。 请注意，我的意思是，我要强调这一点 这一切都是强化学习。 这一切甚至都属于语言模型的范畴。有时你甚至会使用相同的算法，比如 PPO， Sometimes you even use the same algorithms like PPO, 但直觉上来说—— 以及可能你设置超参数的方式将会—— 以及你微调模型的方式会不同。 所以我注意到奖励是稀疏的。 因此如果奖励为 0，你就不会更新参数。 这是正确的吗？ 那我们是不是应该把它设为负一， 这样就朝相反方向走，我们就可以使用那些奖励？ 是的。 所以问题是——好吧，在这种情况下如果奖励是 0， 那么你就不会更新参数。 为什么不把奖励设为负一呢？ 这样至少可以把模型推离 那些错误的回答。 当我们考虑基线时， 这种情况会自动发生。 因为这里——这只是一个简单的朴素策略梯度 在这里你是根据奖励进行更新。 我们将展示还有比仅仅使用奖励更好的方法。 有问题吗？ Question? 我想有一个问题——然后我想那里有人 有一个问题在那边。 ——奖励，只有当我们得到以下回应时才有。

回应的最后一个词。 所以奖励是提示的一个函数 以及整个响应，完整的响应。 并且响应是否正确而已 当我们完全生成它时。 所以我们只有最后一个标记上的梯度吗，还是？ 所以是的——你是说有时会出现这种情况吗 你甚至在完成之前就已经有了奖励。 另一类奖励称为过程监督 或过程奖励，它们基本上允许你持续 追踪你做得有多好。 在那里面你可以采取一些措施。 而且通常来说，如果你的过程奖励（process reward）是好的，那会更好。 为了简化起见，现在我只是默认它是一个结果奖励（outcome reward）。 但绝对地，如果你有更早的奖励， 如果你信任它，你就应该尽早评估该奖励。 as you can if you trust it. 你刚才提到的在语言模型中使用过程奖励的问题在于 并不是—— 要得到一个好的过程奖励真的很难 因为除非你完全偏离正轨， 否则很难判断你做得有多好。 还有其他问题吗？ 同样的问题。 我们这里使用的奖励模型是什么？ 那么我们是如何得到 0 和 1 的？ 那么问题是，奖励模型是什么？ 我会给你展示一些例子。 但你可以考虑奖励模型是怎样的 我想在你的作业中你基本上要实现的是

语言模型会生成一些东西 然后你解析出答案 然后你将答案与答题卡核对 如果匹配则返回 1，否则返回 0。 所以最大化期望回报，策略梯度 做了显而易见的事情。 天真的策略梯度——抱歉，做了显而易见的事情。 但你会遇到高噪声和高方差的问题。 因此——策略梯度方法中的一个关键思想 是使用所谓的基线。 所以我们回顾一下，这是期望奖励。 如果你能实际计算出这个期望奖励 你就相当厉害了。 因为那样你就可以直接计算它。 但你做不到。 所以你改为使用无偏估计 这通常意味着去掉期望， 对……进行采样并去掉期望。 这是一个无偏估计，但其方差 可能相当高。 所以为了举一个玩具例子来体会一下 我所说的高方差是什么意思。 所以假设你只有两个状态，也就是两个 提示，如果你愿意，S1 和 S2。 而在 S1 你有两个可能的动作。 A1 会给你奖励 11。 A2 给你 9 的奖励。 而在状态 S2，a1 给你奖励 0，A2 给你奖励 2。 所以 S1 从根本上来说，给你的奖励更高 而 S2 则只是给你较低的奖励。 当然，最优策略会是 在 S1 时选择 A1，在 S2 时选择 A2。 所以你不希望在 S1 时—— 然后去选择 A2。

但你会注意到这个 9 实际上比这个 S2 要大。 所以你最终可能会遇到这样的情形 每个状态把它想象成一个非常简单的 S1 问题。 而 S2 可能是一个更难的问题。 所以你只是因为某件事变得 So you just because something gets 高回报并不意味着你就应该绝对地对其进行更新 强烈地去针对它。 所以如果 9 大于 2——所以如果你在 进行简单的梯度更新，你或许会选择从 S1 到 A2。 你会觉得哇这太棒了。 我将得到九倍的梯度。 然后我看 S2，A2，我说那只是第二个奖励。 所以我在实际上对其推动较少。 如果你看整个情况， 很明显那也许只是局部的，而不是。 这实际上没有多大意义。 我的意思是，我会强调这是关于期望值的。 如果你观察所有的状态和动作，总体上是一致的。 数学上也都成立。 但你必须把所有东西都考虑进去才能得到那个结论。 这个例子有意义吗？ 在第一种情况下，为什么我们要使用 A2？ 因为 A1 已经有 11 了。 所以问题是，为什么你选择 A2？ 所以你会选择你的策略决定的任何选项。 所以在策略刚开始的时候， 你并不知道哪个回报更高。 因此你可能只是随机选择某个动作。

所以如果你碰巧选择了 A2， 你可能会增加它的权重并获得回报 9。 我的意思是，情况实际上更糟，因为你会说哇，奖励是 9。 这太棒了。 而且你——你越是选择它， 你就越是在针对它进行更新。 然后你就被困在这个局部最优里。 在你甚至无法选择的地方。 在 A1 上不再有概率质量，因为你已经 对 A2 做了太多更新。 所以基线背后的关键思想 是你只是要最大化基线奖励。 我们不是使用期望回报，而是 仅仅减去某个记作 B(S) 的项 称为基线函数。 因此，这只是将期望回报下移了一个常数项 B(S) 的期望。而其优点在于这 by a constant expectation of B of S. And the beauty of this 是因为这个量不依赖于策略 π。 所以即使你把它展开， 你也会得到在状态 S 上对动作 a 的 π 的积分 但在给定状态 S 上对 a 的积分等于 1。 所以这只是—— 它完全不依赖于策略。 因此，当然，优化期望 R 与优化期望 R 减去这个量 是相同的 因为这只是一个常数。这就是基线方法的整个思路。 它保留了优化问题。 现在，如果你在这个新目标上做天真的策略梯度，

基本上你采样一个 S，你采样 A， 然后你只是看这个差值。 所以注意，这是真实的，这对任何 p(S) 都适用。 只要 S 的 P 不依赖于 a，那么 它就不在论证中。 所以它不能依赖 A，那么这是有效的。 那么现在，问题是，在所有可能的 S 的函数 p 中， 我们应该使用哪一个？ 我们再回顾一下这个两个状态的例子，仅仅 为了提供一些直观理解。 我将做出一些假设。 你假设初始策略是在所有状态 S 和动作 A 上的均匀分布， 以及你策略的梯度 最初先暂时忽略这一点。 所以如果你观察梯度的方差， 你会看到有时得到 11，有时得到 9， 有时得到 0，有时得到 2， 而方差是 5.3。 所以如果你定义了一个基线，对 S1 是 10，对 S2 是 1， 那么我们来看看会发生什么。 于是你就从这些奖励值中减去基线。 所以对 S1 你减去 10。 对 S2 你减去 1。 所以现在，基线方差小得多了。 只有 1.1。 所以我们把方差从 5 降到了 1 仅仅通过减去一个基线，这已经相当不错了， 同样地，这在期望上并不会影响你的最终结果。 所以我认为这里有一个假设 即对 S 的概率期望不依赖于策略 π。

所以我在想是否仍然可以 把 B 初始化为，例如，最初的那个冻结策略。 original frozen policy. 好问题。 但 B(S)不应依赖于策略。 你的问题是，我是否可以只取一个初始策略 并将 B(S)设为 S？ 完全可以。 所以这是一种与强化学习相关的事情 你必须有点注意什么是恒定的 以及什么不是。 因此你总是可以拿一个冻结的模型并说那是一个常数。 只要你的表达式不... 依赖于那一点的话，实际上会被视为一个常数， 你不会对它求导，那么一切就没问题。 这也意味着如果你在采样 并且在执行更新， 你必须保留该策略的一个旧副本。 因为有了希望，如果那件事情现在正在变化， 你就会进入这些奇怪的情形 在那里你实际上——它不再是一个常量了。 然后我会接着说， 如果我们把 pi 可视化为上一步的策略，会怎么样， 最后一个半径？ 而不是非常、非常原始的。 所以你可以这样做。 通常，你所做的是本质上有一个策略。 那是一个旧的策略，或是一个锚点，维持一段时间。 或者如果你使用 PPO，你会有一个价值函数， 然后你会暂时固定它一段时间，之后进行一些更新 然后再去更新它。 所以我的意思是，这在实践中就是你所做的。

这其中有一部分是不能依赖于， 对于理论成立来说实际上是怎样的。 但在实践中，人们会做各种各样的事情。 所以我们展示的是，至少就这个例子而言， 根据经验你可以定义某种基线 来降低方差。 所以这有望让你对基线的威力有一个具体的感受 方差越低，收敛越快。 所以你可能会想知道什么是最佳基线可供使用。 如果你做一些数学推导，你会意识到 And if you do some math, you realize 实际上对此有一个非常好的解析解。 这是为单参数模型写的。 如果是多维的，我想你会遇到一些协方差。 而这只是这个导数平方的期望乘以 R 除以用相同表达式但没有 R 的归一化值。 所以这通常很难计算，尤其是 当你的维度很高时。 并且因为这个东西将会是你们某种协方差。 所以通常人们会做的是直接忽略这些东西 然后你会说，好吧，我只是要尝试—— 我要把基线设为在给定 S 时的期望回报。 所以这不是最优的。 但这是一个不错的启发式方法。 即便如此—— 你也无法计算出这个。 因为任何含有期望的东西你都无法精确计算。 你只能估计。 但我现在认为这是一个为你的基线提供良好指导原则的方式。 它就是在给定状态下的期望奖励。

It's the expected reward given your state. 所以简单回顾一下，我们说过我们有朴素的策略梯度。 我们可以引入这个基线，对任何 S 的 B 都成立。 现在，我们得出了一个结论 也许这是一个不错的选择，如果我们 能够尝试掌握在给定 S 时的期望奖励。 所以这个特定的选择有联系 与优势函数相关。 所以在强化学习中，你通常 有一个价值函数，它是在给定 状态时的期望奖励。 然后你有一个 Q 函数，它是 在给定状态下的期望奖励。 并且我采取了一个特定的动作。 注意这里 Q 和 R 是相同的 因为我们假设基于结果的奖励且 a 已 所有的动作。 一般来说，R 会像来自该状态的回报一样，而不仅仅是单一的奖励。 那些现在是相同的。 而优势函数只是 And the advantage function is just 被定义为我采取该行动时所获得的奖励与—— 如果我对可能的行动取平均时所获得的奖励之间的差异。 抱歉，应该说，如果我对可能的行动取平均的话。 具体来说，它在问—— 它在衡量行动 A 比期望值好多少。 我从 S 获得的价值。采取 A 的优势是什么， 而不是仅仅按照基线行事——

抱歉，“基线”这个词有歧义。 但我们是按照策略来做的。 所以如果你看 B 等于这种启发式形式 正如我们之前讨论过的，基线奖励中你只是减去状态 S 的 μ，与优势函数是相同的。 所以你可以这样理解基线—— 如果你有优势——抱歉， 如果你选择了这个基线 if you have this choice of baseline 与优化视角是相同的。 所以这只是为了让你对这个基准方法在做什么有一些直观理解。 总的来说，我们可能会偏离那种做法，因为我们无法估计任何——我们 In general, we are probably going to deviate from that because we can't estimate any of-- we 不能精确计算这些期望值，所以我们 要做些别的事情。 因此，特别是，我将用 delta 来表示 我们放入估计中的任意东西。 所以所有的方法基本上都会采用 策略对数梯度乘以某个东西 那将基于奖励。 所以所有算法，比如你的 PPO、GRPO， 都有这种形式，基本上你 展开并得到一个动作。 那么现在问题是，你想朝那个动作移动多少？ 以及这将取决于奖励。

所以你可以把奖励放在这里。 那就是朴素的策略梯度。 Then that's naive policy gradient. 你可以用奖励减去某个对期望值的估计。 你也可以除以标准差， 这就是 GRPO 所做的，我们稍后会看到。 但这只是一般形式。 因此先验的确切形式并不那么重要 因为明年可能会有 GRPO 2 之类的东西。 但所有这些策略梯度之类的内容 我明年会再讲同样的课程。 说到课程，你可以去看看切尔西的深度强化学习课程， 里面有更多关于推导和直觉的内容以及 强化学习的一般概念。 还有关于策略梯度有什么问题吗 以及基线和优势函数？ 只是关于基线，比如一个基线奖励。 那这就是基线吗？ 那么基础的定义是什么—— 基线是任何仅依赖于状态 S 的函数 B(S) 或者依赖于状态和动作 S 和 A 的函数。你基本上是 把它放进策略梯度算法中。 你可以那样去理解。 所以基本上，你会更新策略梯度 将基于此进行更新。 而基线就是你在那里选择的任何函数。 所以那些见过控制变量的人， 这基本上就是来自统计学的相同想法。 那么现在，让我们尝试更深入地探讨一下。 我们将实际定义一个简单的模型、一个简单的任务，

并实际演示一些代码，这些代码可能看起来像也可能不像第五次作业。 我们也会尽力进行一些运行测试。 And we'll try to hopefully do some runs. 所以很多阐述都是基于 GRPO 算法。 所以这张幻灯片我明年可能得更新。 但它试图将其推广到这里所做的特定选择之外。 所以记住上次 Tatsu 说的关于 GRPO 的内容， So GRPO remember what Tatsu said last time, 它有一条有趣的传承线，你有 PPO 然后 PPO，而 GRPO 是对 PPO 的一种简化，而不是 相比其他算法。 你先从简单开始，然后逐步增加复杂性。 但实际上，我认为 GRPO 存在于 2024 年，而不是 2000 年—— 我记得大概是 17 年左右 PPO 被提出的 是因为语言模型的设定 提供了一些自然的结构，这促使了 GRPO 的出现。 在常规的强化学习场景中， 比如机器人尝试行走，我觉得并不能自然而然地使用 GRPO。 而在语言建模的 场景中，情况的特殊之处在于你有一个提示（prompt），可以从该提示生成一堆 回复。 这为你提供了一个自然的群组结构

来计算本质上的基线，后者 结果是对奖励的平均值。 关键在于你试图最小化方差。 而这个群体是一个自然的比较集合。 这就是为什么这是相对的。 它是相对于群体的。 而如果每次采样的过程看起来都不一样 没有自然的分组结构， 那么 GRPO 就没有太大意义。 你就得找到别的办法，而那“别的办法” 通常往往是一个价值函数， 汇总所有可能的状态和动作。 在这种情况下，你有一个经验估计 涵盖所有可能的响应，而不需要考虑所有细节 这是由于语言模型的设置。 下面是伪代码。 我们过一会儿就用真实的 Python 来查看它。 不过我先在这里快速展示一下。 那么我们来定义一个简单的任务。 我想要一些足够简单的东西 这样我就能在笔记本电脑上训练一个非常简单的模型 没有—— 而运行一个语言模型变换器也做不到。 我是说，我没有一台高档笔记本电脑， 所以那是不可能的。 所以这是一个简单的任务，对 n 个数字进行排序。 所以提示是 n 个数字，响应 是 n 个希望被排序的数字。 现在，你必须定义一个奖励 来完成环境的定义。 而奖励应该反映与已排序的真实结果有多接近 真实就是回应。 因此你会立即看到强化学习中一个有趣的点

是你可以以不同方式定义 奖励。 例如，你可以定义一个奖励 这是一个，如果它已排序则为 1，否则为 0，这大概是 你关心的东西。 但你会记得稀疏奖励问题。 如果你一开始的策略什么也不做， 大多数情况下你会得到 0 奖励， 而你实际上不会取得太大进展。 所以我们来定义一个给予部分奖励的回报。 因此我们将定义一个回报函数，该函数 返回响应与真实答案相匹配的位置数量。 the ground truth. 所以你——基本上，代码看起来是这样的。 你查看真实答案，也就是提示（prompt）。 然后你只需统计响应中与真实答案 相符的位置数目。 因此正确答案的奖励 因为有 4 个位置匹配，所以是 4。 如果你有这种、并不能真正解决任务的东西， 那么奖励是 1。 因为我想你运气好，在第二个位置放了一个 2。 in the second position. 而且你还有这种方法，但它同样没有解决问题。 但你看这个，会说，嗯，它确实在尝试。 再次，你得到 1，因为 0 在正确的位置。 所以这是你可以使用的一种奖励。 也许你对这个不满意，因为它相当偏离目标。 而这更接近。 但它们仍然都得到奖励一。 所以我们来定义一个替代的奖励

给予更多部分性奖励。 所以我在这里要做的是我... 会为响应中出现的每个提示词标记给出一点 在响应中显示出来的提示词。 所以至少你要输出输入中包含的那些词。 这并不过分要求。 因此对于这个特定的例子，我 获得一个值为四的包含奖励。 然后我将为响应中每个已排序的相邻对获得一分。 所以那是 3，因为有 3 对相邻的对。 而奖励是这些分数的总和。 And the reward is the sum. 所以那是 7。 所以如果你注意到了，你会意识到这个奖励实际上存在一个漏洞。 但我会让你自己去发现那个漏洞。 所以现在，这个特别错误的答案， So now, this particular very wrong answer, 它得到 3 分奖励，而这个得到 6 分奖励 因为它真的在尽最大努力。 对于强化学习算法，我们会给予大量部分奖励。 这就是任务。 总的来说，我们会选择第二种奖励 函数。 所以任务很明确。 那是一个环境。 现在，让我们定义一个模型来解决这个任务。 因此，我们将定义一个非常简单的模型。 我们假定提示和回复的长度是固定的并且相同。 当我尝试对示例进行迭代时，曾经有一次—— 我在做一些动态的东西

而那实在令人恼火。 and it was just like annoying to do. 所以我把它简化了。 我将尝试捕捉位置信息 使用独立的位置参数， 因为显然，位置在这里是关键。 或者位置对于排序显然很重要。 我将对响应中的每个位置分别进行解码。 在响应中独立进行解码。 所以这并不是一个回归式模型。 我只是要对序列进行编码。 然后我会把所有的标记分别输出。 这通常不是你在现实生活中想做的事情， 除非我想可能在某些投机性解码中 适量地这样做会有帮助。 但这会让代码变得更加简洁 因为这是自回归生成。 现在，这部分变得非常棘手。 所以尽管这样简化它。 所以这是模型。 所以我有嵌入矩阵，我有—— 对于每个位置，我有一个矩阵 用于基本上将用于解码的转换以及一个矩阵用于—— 抱歉，是编码然后解码。 因此，当我查看前向传播时，这会更容易解释。 所以我从一个提示开始。 我将普遍使用这种表示法 在这里表示张量的维度。 所以有一个批次维度，即示例的数量 以及提示的数量。 然后是一个位置维度，显然是 我所在的位置。 所以我要生成回复。 所以这是推理。 那我们来逐步讲解。 先介绍一些术语。 这里的批量大小是指提示的数量。

每个提示的特定长度为 3。 我将在这种情况下为每个提示生成的响应数量为 2。 而响应的长度为 4。 在排序任务中，这个响应长度 In the sorting task this response length 将与提示长度完全相同。 所以我首先要把这些提示输入模型 以获得 logits（对数几率），这将给出 对于每个提示和每个位置 一个关于标记的分布。 所以这看起来像是我将会 把这些提示嵌入到这个维度中。 然后，我将使用每个提示位置的矩阵进行变换 并且我要把它压缩。 And I'm going to collapse it. 所以使用一个 enzyme（酶）我有批次位置维度 1。 然后这个权重是针对每个位置的 我有这个从维度 1 到维度 2 的变换。 最后我会得到批次维度 2。 所以我已经对位置和维度 1 做了求和。 然后对于每个提示词得到那个向量， 我将在每个位置上应用这个矩阵 并得到一个向量。 最后，针对提示中每个位置的向量， 我会把它转换为词汇表上的 logits。 所以很漂亮——我的意思是这是我编的一个简单模型。 我不知道它是否真的有用—— 有什么用处。 但它在这个玩具例子上是适用的。

所以现在我有了 logits。 这只是为了解决 torch.multinomial 需要扁平化输入的问题的一些花招。 所以我基本上要把它扁平化。 把它变成 batch×位置 × 词表 的形状。 我要对响应进行采样。 I'm going to sample responses. 这会给我批次位置 响应数量，我将称之为 试验就是那个维度。 我基本上会重新排列 以使得对于每个提示， 对于每一次试验，我都会得到一个示例序列。 所以我可能会暂停一下，确认大家都跟上了。 所以这很不错。 这通常是你会调用 VLM 并进行推理的地方。 我这么做只是因为这是一个非常简单的模型。 而且我可以把一切都当作矩阵运算来处理。 生成的响应会非常接近。 我们是在从相同的 logits 中采样。 生成的响应会非常接近。 这些回答会很相近。 所以我们是从该分布独立地采样回答。 从那个分布中采样。 所以它们肯定会很相似。 但这就像从一个语言模型中采样一样。 也一样。 语言模型之间唯一的区别是自回归 而且这是独立的。 相信我。 问题是，我们是否要改变温度，基本上？ 是的。 所以你可以改变温度。 你可以做各种各样的事情。 我在努力忠实于该算法。

于是我得到了我的回答。 我可以对所有这些回答计算一个奖励。 这基本上，就完成了。 对于每一个提示和每一个回复 我把它称为这个奖励函数， 我们之前看过。 没什么特别有趣的地方。 嗯——抱歉，这有点长。 哦，不。 好吧，这部分会少得多 有趣，因为我以为我已经固定了随机种子。 一般来说，你希望奖励有更多的方差 在这些奖励中。 无论如何，所以这些奖励恰好都是 3。 因此这会让其他一些内容变得不那么有趣， 但尽管如此，我会 讲解这里的计算过程。 所以这里有一个叫 compute deltas 的函数， 它把奖励值转换成我要用于更新的 delta（增量）。 而且这个框架有很多方法—— And the framework is there's many ways-- 这里有很多选择，其中一些你将在作业中探索。 例如，如果你可以直接返回 delta 等于奖励， 你可以对奖励进行中心化。 所以你可以对所有回复计算平均值 So you can compute the mean over all of the responses 对于每个提示。 所以这就是它在做的事情。 然后你可以减去均值 这样你就得到了中心化的奖励。

并且回答之前的一个问题，这个 就是如果你的奖励看起来像 1 和一堆 0， 你的平均值会像是，举例来说， 0.1，这意味着如果你减去那个平均值，那么现在， 你会得到一堆 -1 和一个略低于 1 的数。 slightly below 1. 因此，你也将对所有那些负样本进行 更新。 这就是中心化奖励。 然后你可以在 GRPO 中做这样的操作，把奖励规范化。 所以你对奖励进行中心化，然后你 除以标准差并加上一个很小的数。 这样以防万一不会除以零。 然后返回该值。 所以要注意的一点是，如果发生什么情况 当你所有的奖励都是 5 时？ 例如。 所以奖励将只是 5。 所以你会进行一系列更新，以此为中心 奖励将全部为 0。 所以这意味着你不会进行任何更新。 这很有意思。 想象一下如果你有一个例子，其中 你的策略生成的所有内容都获得相同的奖励。 那么你实际上不会对该例子做出任何更新。 on that example. 直觉上，这可能有道理，也可能没有，取决于你想做什么。 但直觉是没有梯度。 你不会——在相对比较这一组时， 没有理由偏好任何一个特定的示例 that group, there's no reason to favor any particular example

比另一个更多。 所以你会选择放弃并希望 其他一些示例能给你一些信号。 而归一化意味着如果你所有的奖励都是， 比如说，10 而所有的奖励都是—— 如果你把所有奖励乘以 100， 那么标准化后的奖励对奖励的尺度是不变的 的。 所以我认为这是它的好处。 只是为了好玩，论文里没有做的另一件事， 但你可能会认为这是件很酷的事。 所以你也可以把每个批次中不是最大值的任何奖励设为零。 对于每个批次也是如此。 这个想法是，在强化学习中经常会出现， 有时你会真正陷入这些局部最优。 如果你给予过多的部分回报， 那么策略就会紧抓住那点回报 去追逐唾手可得的成果，而不是去争取 能给你带来更高回报的事情。 所以你可能会尝试直接说， 如果它没有得到最高奖励， 那么我就不想要。 这是全有或全无的事情。 到目前为止我们所做的是，我们生成了一堆回复， 然后计算奖励。 而这些增量使我能够提供更新，现在， 我需要看另一部分，也就是响应的对数 概率。 因为记住在梯度——策略梯度中 我有增量乘以对数策略的梯度。 这一部分也相当直接。 我把它传入模型， 将 logits 转换成对数概率。 然后有些花招 因为提示并不—— 只是批处理暂停，responses 有一个额外的维度

在里面。 所以我只是把这些对数概率上采样到那个更高维的 张量，维度张量。 然后我要用 responses 来索引对数概率。 所以这些响应基本上对于每个批次试验位置 指定了我需要从对数概率中获取的索引。 所以这是批次试验暂停词汇。 这是带索引的批次试验暂停。 那将给我批次试验暂停。 所以这会给我——模型返回的 logits 为每个位置和提示对所有词表项给出一个分布 以及提示（prompt）。 现在，我得到了这些特定响应的对数概率。 所以这些是我得到的这些特定响应的对数概率。 那么现在，我们终于要计算损失了。 而损失将只是—— 在这里又有一系列选择。 所以我试着把它呈现为某种菜单。 最朴素的做法是——这就是朴素的策略梯度。 你只是取增量和对数概率 然后将它们相乘取平均。 所以这是对数概率。 记住 batch、trial、pause 和 deltas 都是 batch trial。 这是因为它是基于结果的奖励。 你只能得到——没有位置 对增量没有依赖性。 所以基本上，每个位置都会——增量会被 广播到每个位置。 如果你有过程奖励，那么这些增量 也会是按每个位置计算的。 所以那就是最简单的做法。

插一句，确保我们 进入这个“冻结参数”的想法。 稍后我们会在 GRPO 中看到这些比率。 这与这样的想法有关：你 在给定 S 的情况下有一个动作概率 并且你要除以某个其他模型。 非常重要的一点是你 要记得冻结并且不要对 p_old 求导。 所以举个简单的例子。 假设我只有一个参数 w，并且 它同时驱动概率。 把这当作一个策略来考虑。 此外还有一个旧的值，也就是 在这个特定的迭代中就是同样的事情。 现在，如果我计算这个比率会发生什么 然后做一次反向传播。 梯度会是什么？ 所以我是在用 p 除以 p_old。 所以比值将是 1，而 1 的梯度是 0。 那么是什么—— 所以它是 0，这显然没什么用。 正确的做法是， 你像往常一样有你的 p，然后任何你 想把它当作常数处理，就把它放在这个 no grad 里。 然后这就允许你得到——的值。 但不是整个计算图。 然后你把它除以那个。 现在，梯度就不是零了。 就像在你要小心的时候—— 因为在强化学习中，与预训练不同， 存在许多不同的数值，它们都依赖于不同的、要么是旧的要么不是旧的参数。 我们为什么不对结果奖励进行折扣呢 Why don't we discount an outcome reward

当我们回到序列时？ 我想你大概有一个假设 认为最后 100 个标记比最前面的 100 个更重要？ 所以问题是，为什么在有结果奖励时你不做一些折扣—— 进行某种折扣处理？ 也许最后的 100 个标记更重要 而不是前 100 个标记。 我认为这不太明确。 我是说，当然，比如最后那些标记是你在生成、字面上生成答案的地方 you're generating, literally generating the answer 很重要。 但我认为问题在于，强化学习总体上， 你必须进行归因分配（credit assignment）。 早期确实存在会影响结果的决策。 事实上，它们可能比你最终得到的结果更为重要。 因为如果你基本上有了正确的策略， 那么最后发生的事情，可能只是自然而然地发生。 而在强化学习中难点在于 你不知道该如何进行归因分配， 尤其是在只有稀疏奖励的情况下。 所以我认为在这种情形下，我们会 把功劳到处涂抹，或者把责任到处推卸。 所以在这个插曲之后，这对于计算 GRPO 损失是 很重要的。 important for computing the GRPO loss. 所以假设我们有一个旧模型和一个旧的——

所以这是一个旧的检查点。 使用这个旧模型，我们将会 计算响应的对数概率。 所以那就给了我们旧的对数概率。 现在我就跳过未裁剪版本。 所以这是 GRPO 损失。 我们一步步来讲解。 这是裁剪版本。 所以你查看对数概率并且你 除以旧的对数概率，这些 记住应该是一个常数。 实际上，我并没有遵循我自己的建议。 我想我没有把旧的对数概率用 no grad 包裹起来。 但无论如何，你知道该怎么做。 所以你看这个比值，然后 该比值乘以 delta。 因此，如果你相信那个朴素的方法， 你只是把对数概率乘以 delta。 所以这基本上是同一回事， 除了我已经对所有对数做了除法 乘积，这是一个常数。 所以我只是对梯度做了缩放。 但现在，还有另一部分是我在做裁剪。 而我之所以首先计算这个比值的原因是 我要将梯度裁剪到 1 减去 epsilon 和 1 加上 epsilon 之间。 这样做是为了确保比率不会 变得太离谱。 我还会将这些裁剪后的比率乘以 delta。 然后我会取最小值并返回它。 有一个负号，因为我 在谈论奖励时我将其转换成损失。 所以这只是遵循你们 GRPO 论文中的数学推导。 在作业中，你会看到这个解释

为什么这很有道理。 所以如果你的更新在裁剪范围内， 那么你就会得到标准的更新。 但如果你的比率超出范围，那么你的更新幅度 将被限制在 1 减去 epsilon。 以及 1 加上 epsilon。 所以还有这个 KL 惩罚，它提供了 额外的正则化。 现在做一个简短的小数学练习。 记住两个分布之间 KL 的定义。 这是你从左侧分布的第一个样本及其对数值 在左侧或右侧之上。 把它重写为你 Q 除以 P 并带上一个负号。 然后——所以如果你在做 KL 惩罚， 你可以直接使用这个。 但是有这个——如果你想估计 KL， 你可以只去掉期望 这样你就能得到无偏估计。 事实证明，这个特定的估计 在某些情况下可以具有更低的方差，你只需做 Q 对 P 减去对数再减去 1。 所以这看起来有点奇怪，但是你可以 验证这是相同的。 一切相等因为对 P 的 Q 的期望值恰好是 1。 所以这部分只是 1。 然后你减去 1，得到 0。 所以你只剩下这个原始项。 因此数学上成立，内部只是 给你比做这个天真的方法更好的 KL 估计 。 在很多情况下，当你试图 找出这些期望的估计值时， 你通常只是想找到一个无偏的，或有时甚至是

有偏的估计，但其方差要比简单地做那些 天真的方法低。 所以你尝试改写内部以—— 以此来降低方差。 下面是一个关于 KL 惩罚的快速实现。 这相当直接。 唯一需要注意的地方 到当你对最后一个维度求和但取 平均值。 所以你是在对词表求和，但在批次、实验和位置上取平均。 所以到目前为止简单总结一下这些组成部分： So just to summarize so far the components that 需要引入实际算法， 在给定特定固定模型的情况下，你生成响应。 从这些响应中，你计算奖励和增量（deltas）。 在 GRPO 及其相关方法的情况下， 这些不依赖于模型。 它只是对响应的一个函数。 如果你考虑构建这个系统的架构， 有时奖励非常简单。 就像一行代码的精确匹配。 有时奖励会涉及到一个代理， 在环境中执行并做各种疯狂的事情。 所以这可能是昂贵的操作，也可能是廉价的操作， 但它与实际模型无关， 除非你把语言模型作为评判者，在那种情况下，那就是另一个模型。 another model. 与此同时，你可以计算这些回应的对数概率， 然后你根据对数概率来计算损失 以及你在这里得到的差值 把它们结合起来，可以裁剪也可以不裁剪，或者归一化，或者你——

抱歉，这部分在上面。 所以你要么裁剪，要么不裁剪，然后 你进行一次梯度更新。 那么让我们尝试把这一切整合起来。 这就是完整的算法，它 本质上是在代码中实现这里面的内容。 这里有一些样板内容，我就跳过不说。 我将在这里定义一个简单的数据集，包含 3 个提示 每个长度为 3。 然后定义模型。 优化器，然后我这里有这个——有些事情 这里有多个循环以及 GRPO。 所以有一个外层的 epoch 循环。 在这里，如果我在做—— 有时如果我在使用 KL 惩罚， 那么我会冻结模型 并将其作为参考，以便我可以朝那个方向进行正则化。 除此之外，我会对响应进行采样。 所以我们已经讨论过这个。 我们计算奖励。 我们根据你喜欢的变体口味来计算增量。 如果你使用 KL 惩罚的话，那么 你还需要计算参考模型下的对数概率。 如果你不——如果你使用某种裁剪方法的话， 那么你还需要计算当前模型下的对数 概率。 然后你根据这些响应进行若干步操作。 所以这里的想法是，响应通常是这样的。 记住，推理是昂贵的。 你生成然后想要针对这些响应进行一系列梯度步骤。 with respect to the responses. 所以这个内循环基本上就是进行

针对同一组回应采取一系列步骤。 为此，你需要计算对数概率。 你计算损失。 如果愿意，你可以加入一个 KL 惩罚，这有时会有帮助， 有时实际上并不会造成太大差别。 然后你就进行优化。 所以就是——也许我先停下来回答 大家对这里设置有的任何问题。 总体来说，你有外层迭代，也就是 你忽略了 KL 惩罚，因为还有另一个复杂情况 我稍后会提到。 所以你生成了一大堆回复。 然后你对它们快速进行多次迭代。 然后你生成了一大堆回复 并进行一些迭代。 这就是基本思路。 现在，还有一件额外的事情，如果你有 KL 惩罚，那么你实际上会向一个参考模型进行正则化， 该参考模型的变化比这个外部模型更慢。 所以这里的写法实际上 与 GRPO 代码略有不同。 在代码中，实际上有三个循环。 我这里只有两个，因为我只是比较不频繁地 更新参考模型。 我也不会做这部分。 所以有一些实现细节， 但希望你能大致了解它的轮廓 是什么样的。 所以你实际上有三个模型。 你有一个参考模型，你正在对其进行 CL 正则化对比。 然后你还有一个旧模型， 你用它来计算当前模型和旧模型之间的 重要性比率。 你也会把它冻结。 然后在线更新当前模型。

所以关于参考模型的一件令人头疼的事是 你实际上必须存储 整份模型的副本，这实际上会把你的内存翻一倍。 旧模型实际上稍微好一些，因为你 只要记住，如果在代码中，你实际上 不必复制旧模型， 你可以只是生成一堆回答 然后对那些回答计算对数概率 并且只存储对数概率， 因为那些将在内部循环中——你要处理相同的响应。 所以你不必存储模型的完整副本， 你只需要存储对数概率。 you just have to store the log probs. Question? 我们为什么不在旧策略和最新策略之间做 KL 散度，而要针对你的参考策略呢？ 也就是说，问题是，为什么不把 KL 惩罚项针对旧策略而不是参考策略呢？ 这样思考的方式是—— do the kl penalty against the old instead of the reference? The way to think about this is that-- 让我们看看。 所以还记得我们说过在强化学习中我们如何定义目标函数 然后我们去优化它。 这有点像在说谎。 It's a little bit of a lie. 因为实际上你只是定义这个目标函数，

这样你就可以计算梯度。 你计算出梯度并更新一段时间， 但随后你的目标函数会发生变化。 所以实际上你可以把它想成—— 所以在这一点上，你固定一个参考模型 然后你写下这个漂亮的目标 函数，就是—— 让我看看我这儿是不是没有完整的表达式， 但期望奖励以及所有的期望奖励 然后是 KL 惩罚。 所以如果你把它改得很快，那么—— 我想你改得越多， 那么你实际上就越不会去优化 那个目标函数。 所以我想这也许是其中一个原因。 我是说，老实说，我认为从经验上看你—— 这是一个梯度，因为取决于你在这里走多少步 以及多频繁，你可以 让它更像旧策略(pi_old)或更不像旧策略(pi_old)。 我的意思是，你有两种正则化机制。 一种是裁剪机制，应用于 pi old， 另一种是 KL，应用于 pi ref。 问题。 对于最内层循环的第一次迭代 GRPO 旧的 pi_theta 不就是 pi_theta 吗， 这样做是不是很浪费？ 所以第一次迭代的 pi 就等于 pi_theta。 所以你本质上是要—— 嗯，你仍然在执行一次更新。 你只是在按某个因子缩放。 记住，从更新的角度来看，pi 整个这一切 只是一个常数。

我想唯一重要的就是 那样的话你不会被裁剪，因为比率是 1。 所以你就一定会进行一次更新。 所以没问题。 也许就把这点补充完整一点。 实际上——我做了一些实验。 所以你运行我展示的同一段代码。 我要跑 100 个 epoch，epochs 参数设为 10 步。 我们可以看到这条学习曲线。 所以我来大致说明一下它在做什么。 在第 0 个 epoch，第 0 步，我们有这个提示。 它生成了一堆响应内容。 你对它们进行评分，计算奖励， 然后你计算增量。 在这种情况下，它与奖励是相同的。 所以我会更倾向于将参数向那些得分为 3 的方向更新一点，超过 二号。 然后你会看到这个提示也是一样。 这个——我仍然会针对所有这些进行更新—— 朝着这些回复进行，即使它们的奖励都是 3。 然后让我往下滚动看看会发生什么。 最终，你会看到我得到了奖励 3。 但这看起来并不是—— 我的意思是，这并不真的是在按短答案排序。 它生成的数字或多或少与提示中的一致， 但并没有真正进行排序。 让我们看看最后的表现如何。 所以这并不是特别好。 实际上我还没看其他例子。 所以看看这里会发生什么会很令人兴奋。 在这里，我们使用的是中心化奖励

并且可以看到平均奖励在上升。 所以这看起来像是过去的 3 而不是 2，这很有希望。 那么让我们来看看这次运行。 请注意这里我生成了一些示例。 奖励是 2 和 3。 但因为我在做中心化，所以我在给—— 把它们全都推向那些奖励为 3 的样本 并远离那些奖励为 2 的样本。 这就是中心化在起作用。 如果它们都相同，那么中心化其实并没有什么—— 实际上将不会有什么作用。 而之前我所做的更新，我认为有些愚蠢。 因为如果你仔细想想，我在对 1 进行排序， 2、3，以及这里的所有回答都同样糟糕。 所以朝这些方向去更新可能并不太有意义。 我们看看最后效果如何。 所以还是不太行，我想这不算很好。 但它的奖励稍微高了一点。 我可能需要再稍微调一下。 但至少它在这里得到了奖励。 用于获得正确的数值。 而 0、1 并不是最无序的。 在这里——我认为它获得了那个排列。 而在这里，我想它只是卡住了。 到了这一点，你会看到所有的增量都是 0。 所以在这个批次上再多训练其实并不会有帮助。 但如果你重新生成，也许会得到一些新的样本 从而能够进行更新。 所以你可以看到，部分奖励是把双刃剑。 is a double edged sword.

因为我给了你四分。 所以这就像，这是一个不错的奖励。 但我仍然没有完全解决问题。 所以我认为你必须对奖励保持谨慎， 额外的奖励。 然后最后，标准化—— 我觉得这里的归一化实际上并没有起到太大作用。 所以我打算跳过它。 有一点我认为值得注意的是，如果你 看看这些损失曲线。 你会想，这看起来不错， 但为什么即便奖励在上升，这看起来却相当糟糕。 而这又回到我之前的观点： 在这里最小化损失有点儿像在说谎。 that minimizing the loss is a little bit of a lie here. 这并不是说我们在最小化一个可以随时间测量的单一损失函数 因为响应集会随着时间而变化。 你实际上没有一个真正的参照点 来衡量同一个损失。 for the same loss. 我的意思是，你可以在某个验证集上测量损失。 但如果你只有奖励，那么你又只能 依赖于奖励。 因为即便——想想看，甚至 如果你有一堆示例，里面是你作为模型的情况 只是乐于生成它们。 就像损失只与模型正在 生成的内容有关。 所以这是一个自我循环的东西。 因此监控损失并不是真正有意义的事情 去做。 所以也许就到此为止总结一下。

说到强化学习，回过头来， 我认为它真的很令人兴奋，因为它真的 我认为让模型真正超越的关键是 在某种意义上，这是对人类能力的模仿，因为有标签的数据只能 让你在复制标签数据中呈现的行为方面走到这一步。 我认为模型的原则是：如果你能衡量它，就能优化它。 如果你有一个奖励。 If you have a reward. 我认为正如我们已经看到的那样，即使有 这些排序示例，我的意思是 本身也可能具有挑战性。 但也许还有一个更大的问题是，如何 设计不能被破解的奖励。 以及可泛化的环境。 我认为那是一个相当活跃的研究领域。 对于优化部分，我的意思是， 排序并不是那么困难。 我记得我在笔记本上运行了几秒钟。 所以你不应该期待伟大。 我想先回顾一下策略梯度框架， 我认为在概念上是清楚的。 你写下期望回报，减去一个基线， 这就是同一件事。 然后你就尝试去估计那个奖励 或优势。 这是我们没谈到的一件事， 我认为这实际上很重要。 也许明年我应该尝试多做一些这个， 问题在于构建强化学习系统并进行扩展实际上要复杂得多，复杂许多 比预训练要难得多。 原因是你必须进行推理，而推理本身就是一团乱麻。 你还必须管理多个模型。

You also have to manage multiple models. 所以你有正在训练的策略，你有—— 如果你使用 PPO，那么你还有一个评论家。 如果你有推理，并且你 可能有一个推理工作器，那么你就 需要把模型传过去在那里运行推理。 如果你有一个智能体环境， 你有一个需要启动的环境。 在此之上，你还有 pi_old 和 pi_ref。 所以你有多个需要保留的模型。 而且除此之外还得做所有事情 并行分布式地实现这些，并使它们协调运作。 因此还有很多工作要做， 不过在这堂课上我们实际上没有足够的时间 来讨论这些内容。 但如果你感兴趣，欢迎继续深入了解 绝对有我可以发给你的参考资料。 就这些，下次见。
